# Ralph Progress Log
Started: Wed Jan 21 06:13:41 PST 2026

## Codebase Patterns
- Database migrations: Use `_run_migrations()` method in Database class with try/except for ALTER TABLE
- Model fields: Add new fields to dataclasses with defaults to maintain backward compatibility
- Row conversion: Use `len(row) > N` pattern to handle both old and new schema rows
- Enum extensions: New enum values can be added without breaking existing data
- Foreign keys: Make nullable (no foreign key constraint) if record may not exist yet
- Datetime: Use `datetime.now(timezone.utc)` not deprecated `datetime.utcnow()`
- Unused parameters: Prefix with underscore (`_param`) to satisfy linters while keeping interface

---

## 2026-01-21 06:45 - US-001a
- What was implemented:
  - Extended EventType enum with 8 new values (market_anomaly, catalyst_news, etc.)
  - Extended EventStatus enum with 5 new states (new, ongoing, stale, resolved, dismissed)
  - Added 11 new columns to events table (4D scores, user actions, title metadata)
  - Added 6 new columns to observations table (source_url, title, fact_entries, etc.)
  - Created daily_briefs table for storing generated reports
  - Created event_type_history table for tracking event type transitions
  - Added DailyBrief and EventTypeHistory dataclasses to models.py
  - Implemented _run_migrations() for safe schema upgrades
- Files changed:
  - src/tradz/models.py - Added new enums, dataclasses, and fields
  - src/tradz/database.py - Updated schema, insert/query methods, added migration logic
- **Learnings for future iterations:**
  - The `scripts/verify_db.py` script is useful for quick schema verification
  - DuckDB foreign keys are enforced - need to create parent records first
  - Use `SELECT * FROM information_schema.columns WHERE table_name = 'X'` to check schema
  - Keep Event.status default as 'new' (not 'open') for new event system
  - attention_score is now a calculated property on Event, not just Signal
---

## 2026-01-21 - US-009b
- What was implemented:
  - Added TypeScript types for timeline API: TimelineSourceFilter, TimelineObservation, TimelineResponse
  - Added getEventTimeline API function in client.ts with source/limit/offset params
  - Added useEventTimeline hook using TanStack Query's useInfiniteQuery for pagination
  - Created EvidenceTimeline component with all required features:
    - Source filter chips (All, Market, News, SEC, Congress, 13F, Polymarket)
    - Expandable timeline entries showing source icon, timestamp, title, summary
    - Fact entries display in expanded view with grid layout
    - Source URL links open in new tab
    - Load more button with pagination support
    - Empty state when no observations match filter
    - Loading skeleton and error states with retry button
  - Integrated EvidenceTimeline into EventDetail page, replacing old ObservationItem component
- Files changed:
  - frontend/src/api/types.ts - Added timeline types
  - frontend/src/api/client.ts - Added getEventTimeline function
  - frontend/src/hooks/useEvents.ts - Added useEventTimeline hook
  - frontend/src/components/events/EvidenceTimeline.tsx - New component (created)
  - frontend/src/pages/EventDetail.tsx - Integrated EvidenceTimeline, removed old code
- **Learnings for future iterations:**
  - Use useInfiniteQuery for pagination with load more pattern, getNextPageParam returns undefined to stop
  - FilterChip pattern: use active state with cn() for conditional classes
  - Expandable entries: track expanded IDs in a Set for multiple expanded items
  - Always clear expanded state when filter changes for better UX
---

## 2026-01-21 - US-011
- What was implemented:
  - Created QualityGate class with 5 configurable thresholds:
    - min_confidence (70): Minimum confidence score
    - min_sources (2): Minimum unique data sources
    - min_anomaly (50): Minimum anomaly score
    - min_catalyst (40): Minimum catalyst score
    - has_invalidation (true): Require invalidation condition
  - QualityGateEvaluation dataclass returns: passed, gate_score (0-100), failed_gates list, improvement_suggestions
  - TradeIdea dataclass with: direction (long/short/neutral), entry_zone, target, stop_loss, invalidation, time_horizon
  - ResearchPlan dataclass with: questions_to_verify, evidence_to_watch, next_check_date, current_score, gaps_identified
  - TradeIdeaGenerator class: evaluates gates and returns TradeIdea (gates pass) or ResearchPlan (gates fail)
  - Direction determination logic based on flow score and observation types (purchase/sale signals)
  - Price level calculation with entry zone, target (5-15% based on confidence), stop loss (3-7%)
  - Time horizon determination based on event type (filing → position, news → swing)
  - 34 comprehensive unit tests covering all quality gate evaluation scenarios
- Files changed:
  - src/tradz/events/quality_gate.py - New file with all quality gate logic
  - src/tradz/events/__init__.py - Added exports for new classes
  - tests/test_quality_gate.py - New test file with 34 tests
- **Learnings for future iterations:**
  - Use `datetime.now(timezone.utc)` instead of deprecated `datetime.utcnow()`
  - For dataclass default factories with current time, create a helper function `_utcnow()`
  - Prefix unused parameters with underscore (`_observations`) to satisfy linters while keeping interface flexibility
  - Quality gate thresholds can be configured per-user via QualityGateConfig dataclass
  - Gate score calculation uses weighted average: confidence (30%), sources (20%), anomaly (20%), catalyst (20%), invalidation (10%)
---

## 2026-01-21 - US-012
- What was implemented:
  - Created ActionPanel component with TradeIdea and ResearchPlan views:
    - TradeIdea view: direction badge (LONG/SHORT/NEUTRAL), entry/target/stop price levels with percentage, invalidation condition, confidence meter, copy to clipboard
    - ResearchPlan view: questions checklist, evidence to watch, next check date, gaps identified, gate score display
    - GateEvaluationSummary: shows all 5 gates with pass/fail indicators, progress bars, and improvement suggestions for failed gates
  - Added /api/events/{event_id}/recommendation endpoint that evaluates quality gates and returns appropriate recommendation
  - Added TypeScript types for RecommendationResponse, TradeIdea, ResearchPlan, QualityGateEvaluation, GateResult
  - Added useEventRecommendation hook with TanStack Query (5-min stale time, no retry on 404)
  - Integrated ActionPanel into EventDetail page between FactSpotlight and EvidenceTimeline
- Files changed:
  - frontend/src/components/events/ActionPanel.tsx - New component (created)
  - frontend/src/api/types.ts - Added recommendation types
  - frontend/src/api/client.ts - Added getEventRecommendation function
  - frontend/src/hooks/useEvents.ts - Added useEventRecommendation hook
  - frontend/src/pages/EventDetail.tsx - Integrated ActionPanel
  - api/routers/events.py - Added recommendation endpoint
  - api/schemas/events.py - Added Pydantic schemas for recommendation
  - api/services/event_service.py - Added get_event_recommendation method
- **Learnings for future iterations:**
  - Entity IDs in database are not always valid UUIDs (e.g., 'tsla-entity-001') - wrap UUID parsing in try/except
  - When a method is defined outside its class (wrong indentation), TypeScript/pyright reports "Cannot access attribute" error
  - For API types that return None for optional fields, TypeScript interfaces should use `field: Type | null` not `field?: Type`
  - Brutalist design aesthetic: black/white with yellow accent, bold borders (border-2), uppercase labels, box-shadow (shadow-brutal-sm)
  - GateResult type needs both 'name' (display) and 'key' (identifier) fields for proper rendering
---

## 2026-01-21 - US-013a
- What was implemented:
  - Created DailyBriefContent dataclass with structured content for daily briefs:
    - executive_summary: 3 sentences summarizing top 3 events
    - top_events: Top 5 events by attention_score (EventSummary list)
    - trade_ideas: Events that passed quality gates (TradeIdeaSummary list)
    - research_ideas: Events that failed gates but have high potential (ResearchIdeaSummary list)
    - open_loops: Unresolved questions from research plans (OpenLoop list)
    - data_quality: Summary of source health (DataQualitySummary)
    - generation_method: 'claude' or 'template'
  - Implemented DailyBriefGenerator class with generate() method:
    - Sorts events by attention_score descending
    - Evaluates quality gates to separate trade ideas from research plans
    - Generates template-based executive summary from top 3 events
    - Creates open loops from research plan questions
    - Processes system status into data quality summary
    - Converts DailyBriefContent to DailyBrief model for database storage
  - Added supporting dataclasses: EventSummary, TradeIdeaSummary, ResearchIdeaSummary, OpenLoop, SourceHealthSummary, DataQualitySummary
  - 23 comprehensive unit tests covering all functionality
- Files changed:
  - src/tradz/events/daily_brief_generator.py - New file with DailyBriefGenerator and dataclasses
  - src/tradz/events/__init__.py - Added exports for new classes
  - tests/test_daily_brief_generator.py - New test file with 23 tests
- **Learnings for future iterations:**
  - Use `from src.tradz...` not `from tradz...` in test files (matches existing test patterns)
  - Event.attention_score is a property that calculates from 4D scores, can be used for sorting
  - Quality gates return Recommendation with type field to distinguish trade_idea vs research_plan
  - DataQualitySummary tracks overall_status (ok/degraded/error) based on source health counts
  - Research ideas only included if potential_score >= HIGH_POTENTIAL_THRESHOLD (50.0)
---

## 2026-01-21 - US-019
- What was implemented:
  - Added EventActionType enum (pin/unpin/snooze/dismiss/resolve) to models.py
  - Added EventAction dataclass with fields for tracking state changes and user context:
    - id, event_id, action_type, performed_at
    - duration_hours (for snooze), reason (for dismiss)
    - previous_status, new_status, previous_pinned, new_pinned, snoozed_until
    - user_id, user_agent, ip_address (for audit/multi-user support)
  - Created event_actions table in database.py with indexes on event_id and performed_at
  - Added Database methods: insert_event_action, get_event_action_history, get_recent_event_actions
  - Updated EventService.perform_action to log all actions with _log_event_action helper
  - Action logging is fault-tolerant (won't fail the action if logging fails)
  - Verified snoozed events are already filtered from active queries via _build_status_clause
  - 20 comprehensive unit tests covering model, database, service, and integration scenarios
- Files changed:
  - src/tradz/models.py - Added EventActionType enum, EventAction dataclass, _utcnow helper
  - src/tradz/database.py - Added event_actions table and related methods
  - api/services/event_service.py - Updated perform_action to log actions, added _log_event_action
  - tests/test_event_actions.py - New test file with 20 tests
- **Learnings for future iterations:**
  - DuckDB INTERVAL syntax requires string interpolation: `INTERVAL '{hours} hours'` not `INTERVAL ? HOUR`
  - Fault-tolerant logging: wrap log insertion in try/except to avoid breaking the main action
  - Action logging captures state before/after for complete audit trail
  - User context (user_id, user_agent, ip_address) prepared for future multi-user support
---

## 2026-01-21 - US-020
- What was implemented:
  - GET /api/reports/diff endpoint that compares two daily briefs
  - Query params: date (default today), baseline (default yesterday)
  - Response includes: new_events, resolved_events, score_changes, new_trade_ideas, closed_loops
  - Score changes show delta and direction (up/down/unchanged) for events with changes > threshold
  - Handle missing baseline gracefully (returns has_baseline: false with empty arrays)
  - Added Pydantic schemas for BriefDiffResponse and related types (EventScoreChange, NewEventSummary, etc.)
  - Added compare_briefs() method to BriefService with configurable score_change_threshold (default 5.0)
  - Fixed pre-existing bug in reports.py (datetime.timezone.utc -> timezone.utc)
  - 19 comprehensive tests covering all scenarios
- Files changed:
  - api/schemas/briefs.py - Added BriefDiffResponse and related schemas
  - api/services/brief_service.py - Added compare_briefs() method
  - api/routers/reports.py - Added GET /api/reports/diff endpoint
  - tests/test_reports_diff_api.py - New test file with 19 tests
- **Learnings for future iterations:**
  - API endpoints can be added to existing routers (/api/reports/) rather than creating new routers
  - Use Set[str] for efficient membership testing when comparing event lists
  - Score change threshold should be configurable to filter out minor fluctuations
  - Brief comparison logic uses event_id as the key to track changes across briefs
  - The test_briefs_api.py tests had pre-existing failures unrelated to this change
---

## 2026-01-22 - US-021
- What was implemented:
  - Created CompareYesterday component with collapsible UI showing changes since yesterday
  - Added stat cards displaying: X new events, Y resolved, Z score changes
  - New events highlighted with yellow "NEW" badge
  - Score changes show TrendingUp/TrendingDown icons with delta value and current score
  - Resolved events shown with strikethrough and faded opacity
  - Toggle button to show/hide full diff details
  - Detailed sections for: New Events, Score Changes, Resolved Events, New Trade Ideas, Closed Loops
  - Added BriefDiffResponse and related TypeScript types (NewEventSummary, ResolvedEventSummary, EventScoreChange, etc.)
  - Added getBriefDiff API function with optional date/baseline params
  - Added useBriefDiff hook with TanStack Query (5-min stale time, auto-refresh)
  - Integrated CompareYesterday into Dashboard between SystemStatus and SignalInbox
  - Handles no-baseline state gracefully (yesterday's brief not generated yet)
  - Handles no-changes state with success message
- Files changed:
  - frontend/src/api/types.ts - Added BriefDiffResponse and related types
  - frontend/src/api/client.ts - Added getBriefDiff function
  - frontend/src/hooks/useDailyBrief.ts - Added useBriefDiff hook and BRIEF_DIFF_KEY
  - frontend/src/components/events/CompareYesterday.tsx - New component (created)
  - frontend/src/pages/Dashboard.tsx - Integrated CompareYesterday component
- **Learnings for future iterations:**
  - Use collapsible pattern with useState for isExpanded for panel-style components
  - Use separate showDetails state for nested toggle within the component
  - Check has_baseline flag before showing comparison data to handle missing baseline gracefully
  - Strikethrough effect: combine `line-through` with `opacity-60` for resolved/removed items
  - StatCard pattern: icon + count + label for summary metrics
---

## 2026-01-22 - US-022
- What was implemented:
  - Added OpenLoopStatus enum to models.py with 4 states: open, in_progress, resolved, stale
  - Added OpenLoop dataclass to models.py with full persistence fields:
    - id (UUID), event_id (optional UUID), question (str), created_at (datetime)
    - status (OpenLoopStatus), progress_notes (List[str]), resolved_at (Optional[datetime])
  - Created open_loops table in DuckDB with foreign key to events table:
    - id, event_id, question, created_at, status, progress_notes (JSON), resolved_at
  - Added comprehensive CRUD operations to database.py:
    - insert_open_loop, get_open_loop_by_id, get_open_loops (with status filter)
    - get_open_loops_by_event, update_open_loop_status, add_progress_note
    - delete_open_loop, get_stale_open_loops (>7 days old), count_open_loops
  - Updated events/__init__.py to export OpenLoopModel and OpenLoopStatus
  - Two OpenLoop concepts maintained: lightweight brief-specific class for serialization, model-level class for persistence
  - 29 comprehensive unit tests covering model, database, and integration scenarios
- Files changed:
  - src/tradz/models.py - Added OpenLoopStatus enum, OpenLoop dataclass
  - src/tradz/database.py - Added open_loops table, CRUD methods
  - src/tradz/events/__init__.py - Added exports for OpenLoopModel, OpenLoopStatus
  - tests/test_open_loops.py - New test file with 29 tests
- **Learnings for future iterations:**
  - Keep separate OpenLoop classes for different concerns: lightweight for brief serialization, full model for persistence
  - Progress notes stored as JSON array in database, converted via json.dumps/json.loads
  - Stale threshold for open loops: >7 days without resolution
  - Foreign key to events table is optional (event_id can be None for manual loops)
  - Use OpenLoopModel alias in events/__init__.py to distinguish from brief's OpenLoop
---

## 2026-01-22 - US-023
- What was implemented:
  - GET /api/loops endpoint with status filtering (all, open, in_progress, resolved, stale)
  - GET /api/loops/{loop_id} for detailed loop info with full progress notes history
  - POST /api/loops to create new loops (optionally linked to events via event_id)
  - PATCH /api/loops/{loop_id} to update status or add progress notes
  - DELETE /api/loops/{loop_id} to remove loops
  - LoopService class for business logic:
    - Validates event exists when event_id provided
    - Enriches responses with event summaries (title, attention_score, status)
    - Validates loop existence before updates/deletes
  - Pydantic schemas: OpenLoopListItem, OpenLoopDetail, CreateOpenLoopRequest, UpdateOpenLoopRequest, etc.
  - Added Database.get_event_by_id method for foreign key validation
  - 26 comprehensive API tests using mock service pattern
- Files changed:
  - api/routers/loops.py - New router with all endpoints
  - api/schemas/loops.py - New Pydantic schemas
  - api/services/loop_service.py - New service layer
  - api/routers/__init__.py - Added loops_router export
  - api/main.py - Added loops router to app
  - src/tradz/database.py - Added get_event_by_id method
  - tests/test_loops_api.py - New test file with 26 tests
- **Learnings for future iterations:**
  - Follow existing test patterns: mock service at router level (patch api.routers.loops.loop_service)
  - Mock database before importing app module (prevents singleton issues)
  - Include event summary in loop responses for better UX (avoids extra API calls)
  - Validate foreign key references (event_id) in service layer, not at database level
  - Use status filters as enums in both schemas and service for type safety
---

## 2026-01-22 - US-024
- What was implemented:
  - OpenLoops component with interactive checklist for tracking open questions
    - Collapsible panel with header showing total count and stale count badges
    - Status indicators (OPEN/IN PROGRESS/RESOLVED/STALE) with color coding
    - Checkbox to mark loops as resolved with optimistic UI updates
    - Text input to add progress notes with keyboard support (Enter to submit, Escape to cancel)
    - Links to related event detail pages via react-router-dom
    - Stale loops (>7 days old) highlighted with yellow border and background
    - Create form to add new open questions
    - Empty state with helpful guidance
    - Loading and error states
  - useOpenLoops hook using TanStack Query:
    - useOpenLoops for fetching list with status filter
    - useOpenLoopDetail for single loop details
    - useCreateOpenLoop, useUpdateOpenLoop, useDeleteOpenLoop mutations with optimistic updates
    - useOpenLoopActions convenience hook with typed methods (resolve, startProgress, addNote, delete)
  - TypeScript types added to frontend/src/api/types.ts:
    - OpenLoopStatusValue, OpenLoopEventSummary, OpenLoopAPIItem, OpenLoopsListResponse
    - OpenLoopDetail, CreateOpenLoopRequest, UpdateOpenLoopRequest, etc.
  - API client functions in frontend/src/api/client.ts:
    - getOpenLoops, getOpenLoopById, createOpenLoop, updateOpenLoop, deleteOpenLoop
  - Integrated OpenLoops into DailyBrief component, replacing static OpenLoopCard
- Files changed:
  - frontend/src/components/events/OpenLoops.tsx - New interactive component
  - frontend/src/hooks/useOpenLoops.ts - New hooks file with TanStack Query
  - frontend/src/api/types.ts - Added Open Loops TypeScript types
  - frontend/src/api/client.ts - Added loops API functions
  - frontend/src/components/events/DailyBrief.tsx - Updated to use OpenLoops component
  - frontend/src/components/events/index.ts - Added OpenLoops export
- **Learnings for future iterations:**
  - TypeScript exhaustive switch needs no default case when all enum values are covered
  - Use optimistic updates with TanStack Query for responsive UI (update local state immediately, rollback on error)
  - setQueriesData with query key pattern can update multiple cached queries at once
  - Import react-router-dom Link for internal navigation links
  - Relative time formatting helps users quickly understand staleness
---
